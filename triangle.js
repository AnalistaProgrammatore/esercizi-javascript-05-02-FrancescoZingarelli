for (let i = "#"; i.length <= 7 ; i = i + "#") console.log(i);

/*

l'ho fatta così per provare a vedere se potevo farla in una riga
sennò l'avrei fatta così:

let triangleRow = "";
for (let i = 0; i <= 6 ; i++) {
    triangleRow += "#";
    console.log(triangleRow);
}

*/
